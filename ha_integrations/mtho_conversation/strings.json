{
  "config": {
    "step": {
      "user": {
        "title": "MTHO Conversation einrichten",
        "description": "MTHO API URL und Bearer-Token (HA_WEBHOOK_TOKEN aus .env) eingeben.",
        "data": {
          "base_url": "MTHO API URL",
          "token": "Bearer Token (HA_WEBHOOK_TOKEN)"
        }
      }
    },
    "error": {
      "cannot_connect": "MTHO API nicht erreichbar. URL und Token prüfen (HA_WEBHOOK_TOKEN in .env)."
    },
    "abort": {
      "already_configured": "MTHO ist bereits für diese URL konfiguriert."
    }
  }
}
