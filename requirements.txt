# CORE SYSTEM — Python-Abhängigkeiten (kein Ersatz für Architektur-Doku).
# Kanon: KANON_EINSTIEG.md · Index: docs/BIBLIOTHEK_KERN_DOKUMENTE.md · Agenten: AGENTS.md · Regeln: .cursorrules
pydantic>=2.7.0
python-dotenv>=1.0.0
loguru>=0.7.0    # Besseres Logging als Standard-Lib

# NETWORK & API
requests>=2.31.0
httpx>=0.27.0    # Für asynchrone API Calls (Scout/HA)
paramiko>=3.4.0  # SSH (VPS, Scout, … — siehe .env VPS_SSH_KEY / Doku)
fastapi>=0.110.0 # OMEGA/CORE FastAPI-Backend (:8000)
uvicorn>=0.29.0  # Server Runner
python-multipart>=0.0.9 # File-Upload (Dictate-Route)

# FRONTEND
streamlit>=1.32.0
streamlit-autorefresh>=1.0.0

# DATA HANDLING
pandas>=2.2.0
numpy>=1.26.0

# AI INTERFACE
ollama>=0.1.8    # Offizielle Python Lib
langchain>=0.1.16 # Update für Python 3.14 Compat
langchain-core>=0.1.45 # Explizites Core Update
rapidfuzz>=3.0.0 # Fuzzy-Matching für Voice Command Parser
anthropic        # Offizieller Anthropic-Client für Claude 4.6

# BACKUP (daily_backup.py: .env-Verschlüsselung)
cryptography>=42.0.0

# MCP (stdio): z. B. src.scripts.mcp_core_chroma_stdio — Config: mcp_remote_config.json, Doku: AGENTS.md
mcp>=1.2.0

# TESTING
pytest>=8.0.0
