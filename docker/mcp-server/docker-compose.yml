version: '3.8'

services:
  mcp-server:
    build: .
    container_name: mcp-server
    restart: unless-stopped
    ports:
      - "8001:8001"
    volumes:
      - /opt/mtho-core/workspace:/workspace
    networks:
      - mtho_net

networks:
  mtho_net:
    external: true
