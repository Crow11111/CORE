# /etc/default/omega-backend — nur auf dem VPS; nicht ins Repo committen.
# Kopie: siehe vps_deploy_omega_backend.py (scp nach /etc/default/omega-backend).
#
# Beispiele (Werte vom Operator setzen):
# GEMINI_API_KEY=
# DATABASE_URL=
# CHROMA_HOST=127.0.0.1
# CHROMA_PORT=32779
# HASS_URL=
# HASS_TOKEN=
