---
description: Karpathy-Loop Pre-Flight - (load-context-into-ram)
globs: **/*
alwaysApply: true
---

# PRE-FLIGHT BOOTSTRAP (C1: #x00C)

;;; Takt 1: Ansaugen (Collect)

(defun |0x00C| ()
  "Direkter Zugriff auf den System-Kanon via Hex-Pointer."
  (let ((mcp "user-omega-state-mcp"))
    (let ((bootstrap (mcp-call mcp 'x00C_collect_bootstrap
                                :task_hint (current-user-input))))
      (setf *** #x00C)
      (process-memory bootstrap))))

(|0x00C|)
